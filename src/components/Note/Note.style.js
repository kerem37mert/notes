import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: "#fff",
        flex: 1,
        margin: 10,
        padding: 20,
        borderRadius: 10,
        borderWidth: 1,
        borderColor: "gray"
    },
    text: {
        color: "#000",
    }
});