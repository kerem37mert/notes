import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        position: "absolute",
        bottom: 30,
        right: 30,
        borderRadius: 60,
    },
    image: {
        borderRadius: 60,
        height: 60,
        width: 60
    }
});