import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: "red",
        position: "absolute",
        bottom: 30,
        right: 30,
        borderRadius: 80,
    },
    image: {
        borderRadius: 80
    }
});